backend:
  name: git-gateway
  branch: main

# Où seront stockées les images
media_folder: "images/uploads"
public_folder: "/images/uploads"

# Collections = types de contenu
collections:
  - name: "articles"
    label: "Articles"
    folder: "content/articles"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"

    fields:
      - label: "Titre"
        name: "title"
        widget: "string"

      - label: "Catégorie"
        name: "category"
        widget: "select"
        options:
          - "International"
          - "Politique"
          - "Économie"
          - "Université"

      - label: "Auteur"
        name: "author"
        widget: "string"
        default: "Rédaction Sphère Diplomatique"

      - label: "Date"
        name: "date"
        widget: "datetime"

      - label: "Image"
        name: "image"
        widget: "image"
        required: false

      - label: "Contenu"
        name: "body"
        widget: "markdown"
